---
title: Introductie
---

import { Gear,  Plus,  FloppyDisk,  MagnifyingGlass, ScribbleLoop, Pen, Path, ArrowCounterClockwise, ArrowClockwise, Hand, Wrench, Lock} from "@phosphor-icons/react";
import { Tabs, TabItem } from '@astrojs/starlight/components';
import logo from '/public/img/logo.svg';

![Banner](/img/banner.png)

---

> Welkom bij Butterfly, de opensource notitie app.

Butterfly is een aantekening bij het nemen van de app die gericht is op uniforme ervaring op alle platformen. Het biedt eenvoudige UI met krachtige aanpassingen.

## Aan de slag

> Je kunt de zijbalk gebruiken om snel door de wiki te navigeren.

Om te beginnen met werken aan de app heb je twee opties:

1. [Downloads](/downloads) de app versie die geschikt is voor je platform (Butterfly kan worden uitgevoerd op de meeste mobiele, desktop- en webplatforms).
2. Gebruik [webversie](https://web.butterfly.linwood.dev) als je je platform niet kunt vinden of als je het niet op wilt installeren. Zie [here](opslag#web) over bestandsopslag op de webversie.

> Zodra je de app opent, ga verder met lezen.

## Hoofd weergave

![Hoofd weergave](main.png)

De hoofdweergave bestaat uit verschillende elementen

<Tabs syncKey="platform">
    <TabItem label="Desktop">
        ![Welcome screen](welcome_screen_desktop. ng)
        Het bovenste scherm houdt de banner ingedrukt voor updates, de link naar deze documenten, en het <Gear className="inline-icon"/> configuratiepictogram. Tot slot, rechts van het <Gear className="inline-icon"/> config symbool heb je een dropdown menu voor controle wanneer deze banner verschijnt (altijd/nooit/bij updates).
        Het "Bestanden" gedeelte in het grote gebied van links. Daar je kan de gebruikelijke operaties doen:
        - Selecteer het weergavetype (raster of lijst)
        - Selecteer de [bron opslag](.. opslag) van de bestanden
        - Sorteer de weergavevolgorde van de bestanden
        - Voeg een map toe, bestand, sjabloon, of importeer bestanden door te klikken op de <Plus className="inline-icon"/> plus teken
        - Typ het directe pad naar een gewenste locatie (de locatie veld),
        - Zoeken naar bestanden
        tot slot naar rechts, U heeft het snelle startscherm met de templates die beschikbaar zijn op de huidige locatie. Voor een nieuwe installatie bevat dit het lichte en donkere sjabloon
    </TabItem>
    <TabItem label="Mobile">
        Bij het openen van Butterfly op een mobiel apparaat wordt u een scherm gepresenteerd zoals deze
        ! Welkom scherm eerste deel](welcome_screen_mobile_1. ng)
        De banner voor updates vasthouden en hieronder kunt u de documnentatie link en daarna het configuratiepictogram van de <Gear className="inline-icon"/> vinden. Tot slot, rechts van het <Gear className="inline-icon"/> config symbool heb je een dropdown menu voor controle wanneer deze banner verschijnt (altijd/nooit/bij updates).
        Hieronder vindt u de snelle startsectie waar u de sjablonen kunt onderhouden. Door defaule, de sjablonen zijn de lichte en donkere sjablonen.
        Scrollen naar beneden. Je ziet de bestandsinterface:
        \
        ! Welkom scherm tweede deel](welcome_screen_mobile_2. ng)  
        De "Bestanden" sectie in het grote gedeelte van links. Daar je kan de gebruikelijke operaties doen:
        - Selecteer het weergavetype (raster of lijst)
        - Selecteer de [bron opslag](.. opslag) van de bestanden
        - Sorteer de weergavevolgorde van de bestanden
        - Voeg een map toe, bestand, sjabloon, of importeer bestanden door te klikken op de <Plus className="inline-icon"/> plus teken
        - Typ het directe pad naar een gewenste locatie (de locatie veld),
        - Zoek naar bestanden
 </TabItem>
</Tabs>

Eenmaal een document openen door een sjabloon of een bestaand bestand te selecteren, kunt u naar **Document Weergave** gaan

## De Document Weergave

U kunt terugkeren van de documentweergave naar de documentenlijst met de back-actie van uw apparaat. Net als bij de hoofdweergave, wordt het document gewijzigd op basis van uw apparaat.

<Tabs syncKey="platform">
    <TabItem label="Desktop">
  In the Desktop view, you'll have at the top left the file bar\
  ![file_bar](document_view_file_bar.png)\
  This holds, from left to right, the <img alt="logo" src={logo.src} className="inline-icon" width="16"/> butterfly button to open the menu. Hierna is het naam veld dat de naam van de notitie weergeeft. U kunt de notitie hernoemen door een nieuwe naam in dat veld in te typen en deze op te slaan met de <FloppyDisk className="inline-icon"/> save knop. Ten slotte kan je <MagnifyingGlass className="inline-icon"/> naar elementen binnen de notitie zoeken.
  \
  rechtsboven het scherm heb je de werkbalk\
  ![toolbar](document_view_toolbar. ng)\
  Standaard heeft deze balk de <ScribbleLoop className="inline-icon"/> lasso tool; de <Pen className="inline-icon"/> pen tool; het pad <Path className="inline-icon"/> wis tool; de <ArrowCounterClockwise className="inline-icon"/> undo and <ArrowClockwise className="inline-icon"/> redo butons, en de <Hand className="inline-icon"/> hand tool. Dan heb je de knop <Plus className="inline-icon"/> om elementen toe te voegen aan de werkbalk, de knop <Wrench className="inline-icon"/> om het document te configureren. en tot slot de <Lock className="inline-icon"/> tool om de zoom en/of de positie van het scherm te bekijken.
  ### Belangrijke opmerkingen
  1. Als sommige van het element op de werkbalk niet zichtbaar zijn, klikt u op en sleep (of veeg) naar links en rechts om verdere hulpmiddelen te onthullen.
  2. Na het selecteren van een tool zal een klein menu verschijnen. Als u weer lang klikt op het gereedschap kunt u het naar een nieuwe positie slepen
  3. Je kunt meer gereedschappen toevoegen aan de werkbalk door op de <Plus className="inline-icon"/> plus teken te klikken.
 </TabItem>
 <TabItem label="Mobile">
  In de mobiele weergave je hebt bovenaan de bestandsbalk\
     ![file_bar](document_view_file_bar. ng)\
  Deze houdt, van links tot rechts, de <img alt="logo" src={logo.src} class="inline-icon" width="16"/> vlinder knop om het menu te openen. Hierna is het naam veld dat de naam van de notitie weergeeft. U kunt de notitie hernoemen door een nieuwe naam in dat veld in te typen en deze op te slaan met de <FloppyDisk className="inline-icon"/> save knop. Ten slotte kan je <MagnifyingGlass className="inline-icon"/> naar elementen binnen de notitie zoeken.
  \
  Aan de onderkant van het scherm heb je de werkbalk\
  ![toolbar](document_view_toolbar. ng)\
  Standaard heeft deze balk de <ScribbleLoop className="inline-icon"/> lasso tool; de <Pen className="inline-icon" /> pen tool; het pad <Path className="inline-icon"/> wis tool; de <ArrowCounterClockwise className="inline-icon"/> undo and <ArrowClockwise className="inline-icon"/> redo butons, en de <Hand className="inline-icon"/> hand tool. Dan heb je de knop <Plus className="inline-icon"/> om elementen toe te voegen aan de werkbalk, de knop <Wrench className="inline-icon"/> om het document te configureren. en tot slot de <Lock className="inline-icon"/> tool om de zoom en/of de positie van het scherm te bekijken.
  ### Belangrijke opmerkingen
  1. Als sommige van het element op de werkbalk niet zichtbaar zijn voor jou, of veeg naar links en rechts om verdere elementen te onthullen.
  2. Na het selecteren van een tool verschijnt een klein menu om de tool te configureren. Als u weer lang klikt op het gereedschap kunt u het naar een nieuwe positie slepen
  3. Je kunt meer gereedschappen toevoegen aan de werkbalk door op de <Plus className="inline-icon"/> plus teken te klikken.
 </TabItem>
</Tabs>
